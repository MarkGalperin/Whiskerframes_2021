function [outputArg1,outputArg2] = conf2error(x,mode)
%CONF2ERROR Returns mean error at configuration based the operating mode
%   Takes:
%           x: [1x3] configuration of the control frame
%           mode: (str) the error calculation mode 
%
    outputArg1 = inputArg1;
    outputArg2 = inputArg2;
end

